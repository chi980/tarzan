spring:
  application:
    name: tarzan

  datasource:
    url: jdbc:postgresql://http://43.203.126.22/tarzan
    username: postgres
    password: '@strawberry_tarzan_8960!'
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
      dialect: org.hibernate.spatial.dialect.postgis.PostgisDialect
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    use_sql_comments: true
    database: postgresql

  batch:
    job:
#      name: ${job.name:NONE}
      enabled: true
    jdbc:
#      initialize-schema: never
       initialize-schema: always

  web:
    resources:
      static-locations: classpath:/static
